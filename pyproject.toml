[project]
name = "campus-treasure"
version = "0.1.0"
description = "NTUST campus treasure hunt: scan-to-login, scan-to-open-question, scored answers"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "fpdf2>=2.8.8",
    "python-dotenv>=1.2.3",
    "pyyaml>=6.0.3",
    "segno>=1.6.6",
    "waitress>=3.0.2",
]
